# Python dependencies for inference and image processing
# Note: install `torch` according to your CUDA/CPU configuration (see README)
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
Pillow>=10.0.0

# Roboflow / Inference SDK (optional for remote model hosting)
inference-sdk>=0.22.0
roboflow==1.2.2
